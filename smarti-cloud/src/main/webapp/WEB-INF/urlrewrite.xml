<?xml version="1.0" encoding="UTF-8"?>

    <!DOCTYPE urlrewrite
        PUBLIC "-//tuckey.org//DTD UrlRewrite 4.0//EN"
        "http://www.tuckey.org/res/dtds/urlrewrite4.0.dtd">

    <urlrewrite>

        <rule enabled="true">
           <condition name="host" operator="notequal">dajana.ca</condition>
           <condition name="host" operator="notequal">home.dajana.ca</condition>    
           <condition name="host" operator="notequal">localhost</condition>                     
           <from>^/resources/(.*)$</from>
           <to type="proxy">http://localhost/resources/$1</to>
        </rule>

        <rule enabled="true">
           <condition name="host" operator="notequal">dajana.ca</condition>
           <condition name="host" operator="notequal">home.dajana.ca</condition>    
           <condition name="host" operator="notequal">localhost.net</condition>
           <from>^/site/(.*)$</from>
           <to type="redirect">/proxy/$1</to>
        </rule>
       
    </urlrewrite>